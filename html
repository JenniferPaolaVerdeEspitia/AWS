<!DOCTYPE html>
<html lang="es" class="h-100">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Calculadora Inteligente (IA)</title>

  <!-- Bootstrap & Icons -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"/>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css"/>

  <style>
    :root{
      --bg-1:#0f172a;            /* slate-900 */
      --bg-2:#1e293b;            /* slate-800 */
      --pri-1:#7c3aed;           /* violet-600 */
      --pri-2:#22d3ee;           /* cyan-400 */
      --acc-1:#34d399;           /* emerald-400 */
      --err-1:#ef4444;           /* red-500 */
      --white:#ffffff;
      --muted:#9ca3af;
      --glass-bg: rgba(255,255,255,.06);
      --glass-bd: rgba(255,255,255,.18);
      --radius: 18px;
      --shadow-1: 0 10px 30px rgba(2,8,23,.35);
      --shadow-2: 0 16px 60px rgba(2,8,23,.55);
    }

    /* ===== Fondo con gradiente animado ===== */
    body{
      display:flex; flex-direction:column; min-height:100vh; color:var(--white);
      background: radial-gradient(1200px 800px at 10% -10%, rgba(124,58,237,.25), transparent 55%),
                  radial-gradient(1000px 700px at 110% 10%, rgba(34,211,238,.18), transparent 55%),
                  linear-gradient(135deg, var(--bg-1), var(--bg-2));
      animation: bgMove 18s ease-in-out infinite;
      background-attachment: fixed;
    }
    @keyframes bgMove{
      0%   {background-position: 0 0, 0 0, 0 0;}
      50%  {background-position: 2% 1%, -1% 3%, 0 0;}
      100% {background-position: 0 0, 0 0, 0 0;}
    }

    /* ===== Navbar gradiente + brillo ===== */
    .navbar{
      background: linear-gradient(90deg, rgba(124,58,237,.9), rgba(34,211,238,.85));
      box-shadow: var(--shadow-2);
      backdrop-filter: blur(6px);
    }
    .navbar-brand{
      font-weight:700; letter-spacing:.3px;
      text-shadow: 0 2px 20px rgba(34,211,238,.35);
    }

    /* ===== Contenedor principal ===== */
    main{ flex-shrink:0; }

    /* ===== Tarjeta efecto “glass” ===== */
    .card-glass{
      background: var(--glass-bg);
      border:1px solid var(--glass-bd);
      border-radius: var(--radius);
      box-shadow: var(--shadow-1);
      backdrop-filter: blur(10px);
      transition: transform .35s ease, box-shadow .35s ease, border-color .35s ease;
    }
    .card-glass:hover{
      transform: translateY(-4px);
      box-shadow: 0 20px 60px rgba(2,8,23,.65);
      border-color: rgba(255,255,255,.28);
    }

    /* ===== Título con gradiente y aparición ===== */
    .title-gradient{
      background: linear-gradient(90deg, var(--pri-2), var(--pri-1));
      -webkit-background-clip:text; background-clip:text; color:transparent;
      text-shadow: 0 2px 24px rgba(124,58,237,.25);
      animation: fadeUp .7s ease both .05s;
    }
    .lead-muted{ color:var(--muted); animation:fadeUp .7s ease both .15s; }
    .tiny-muted{ color:#a1a1aa; animation:fadeUp .7s ease both .25s; }

    @keyframes fadeUp{
      from{ opacity:0; transform: translateY(8px) scale(.98); }
      to  { opacity:1; transform: translateY(0)   scale(1); }
    }

    /* ===== Textarea estilizada ===== */
    #textoOperacion{
      background: rgba(15,23,42,.55); color:var(--white);
      border:1px solid rgba(148,163,184,.25);
      border-radius: calc(var(--radius) - 6px);
      transition: border-color .25s ease, box-shadow .25s ease, transform .12s ease;
    }
    #textoOperacion:focus{
      border-color: rgba(124,58,237,.7);
      box-shadow: 0 0 0 4px rgba(124,58,237,.25), 0 10px 30px rgba(2,8,23,.55) inset;
      transform: translateY(-1px);
    }

    /* ===== Botón principal con “pulse” ===== */
    #btnCalcular{
      --btn-grad: linear-gradient(90deg, var(--pri-1), var(--pri-2));
      background: var(--btn-grad); border:none; border-radius: 14px;
      box-shadow: 0 10px 25px rgba(124,58,237,.35), inset 0 -2px 0 rgba(255,255,255,.08);
      transition: transform .12s ease, filter .25s ease, box-shadow .25s ease;
    }
    #btnCalcular:hover{ filter: brightness(1.05); box-shadow: 0 14px 34px rgba(34,211,238,.35); }
    #btnCalcular:active{ transform: scale(.98); }
    #btnCalcular .bi{ transform: translateY(1px); }

    /* ===== Alertas con slide-in ===== */
    .alert{
      border:1px solid rgba(255,255,255,.18);
      border-radius: 14px;
      animation: slideIn .45s ease both;
    }
    .alert-success{
      background: rgba(16,185,129,.12);
      border-color: rgba(52,211,153,.35);
      color:#d1fae5;
    }
    .alert-danger{
      background: rgba(239,68,68,.12);
      border-color: rgba(239,68,68,.35);
      color:#ffe5e5;
    }
    @keyframes slideIn{
      from{ opacity:0; transform: translateY(8px); }
      to  { opacity:1; transform: translateY(0); }
    }

    /* ===== Footer sutil ===== */
    .footer{
      margin-top:auto;
      background: linear-gradient(90deg, rgba(2,6,23,.85), rgba(2,6,23,.7));
      border-top:1px solid rgba(255,255,255,.08);
      backdrop-filter: blur(6px);
    }

    /* decoraciones suaves */
    .blob{
      position: absolute; filter: blur(60px); opacity:.45; z-index:-1;
      border-radius: 999px; pointer-events: none;
      animation: float 12s ease-in-out infinite;
    }
    .blob.one{ width:320px;height:320px;background: #8b5cf6; top:-60px; left:-40px; }
    .blob.two{ width:260px;height:260px;background: #22d3ee; bottom:-60px; right:-30px; animation-delay: -4s; }
    @keyframes float{
      0%,100%{ transform: translateY(0) translateX(0) }
      50%{ transform: translateY(8px) translateX(6px) }
    }
  </style>
</head>

<body class="d-flex flex-column min-vh-100">
  <!-- blobs decorativos -->
  <div class="blob one"></div>
  <div class="blob two"></div>

  <nav class="navbar navbar-expand shadow-sm">
    <div class="container">
      <a class="navbar-brand d-flex align-items-center" href="#">
        <i class="bi bi-robot fs-4 me-2"></i>
        <span>Calculadora IA</span>
      </a>
    </div>
  </nav>

  <main class="container my-5">
    <div class="row">
      <div class="col-lg-8 offset-lg-2">

        <div class="text-center mb-4">
          <h1 class="display-5 fw-bold title-gradient">Calculadora IA</h1>
          <p class="lead lead-muted">Escribe cualquier operación aritmética en lenguaje natural.</p>
          <p class="tiny-muted">(Ej: “raíz cuadrada de 81” o “5 elevado a la 3”)</p>
        </div>

        <div class="card card-glass">
          <div class="card-body p-4 p-md-5">

            <div class="mb-3">
              <label for="textoOperacion" class="form-label fs-5">Tu consulta:</label>
              <textarea id="textoOperacion" class="form-control form-control-lg"
                        rows="3" placeholder="Ej: ¿cuánto es (15 × 4) ÷ 2?"></textarea>
            </div>

            <div class="d-grid">
              <button id="btnCalcular" class="btn btn-lg text-white" type="button">
                Calcular <i class="bi bi-send ms-1"></i>
              </button>
            </div>

            <div id="divResultado" class="alert alert-success mt-4 mb-0" style="display:none" role="alert">
              <h4 class="alert-heading mb-0">
                <i class="bi bi-check-circle-fill me-2" style="color:var(--acc-1)"></i>
                El resultado es: <span id="resultadoTexto" class="fw-bold"></span>
              </h4>
            </div>

            <div id="divError" class="alert alert-danger mt-4 mb-0" style="display:none" role="alert">
              <i class="bi bi-exclamation-triangle-fill me-2" style="color:var(--err-1)"></i>
              <span id="errorTexto">Hubo un error al procesar la solicitud.</span>
            </div>

          </div>
        </div>

      </div>
    </div>
  </main>

  <footer class="footer text-white text-center p-4">
    <div class="container">
      <p class="mb-0 small opacity-75">&copy; 2025 | Desarrollado por Jennifer Paola.</p>
    </div>
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <script src="script.js"></script>
</body>
</html>
